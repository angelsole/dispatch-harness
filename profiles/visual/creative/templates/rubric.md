<!-- Template. Copy to `.creative/rubric.md` in the target repo. Without one the
     critic falls back to the profile's `creative/rubric.md`, which is generic —
     and generic is the weakest form of this artefact. Rewrite every anchor in
     the language of THIS project: the critic can only name a fault it has been
     given words for. -->

# <project> — visual rubric

The six axes the critic scores 1–5. Scores are **advisory**: the signals worth
acting on are the deterministic checks and the pairwise verdict against the
champion, because a VLM's absolute aesthetic score is calibrated by nothing.

| Axis | 1 | 3 | 5 |
|---|---|---|---|
| `palette_discipline` | | | |
| `grid_discipline` | | | |
| `silhouette_readability` | | | |
| `composition` | | | |
| `animation_continuity` | | | |
| `style_match_to_reference` | | | |

Write each anchor as something visible in a frame, not as an adjective. "Windows
are three colours from the palette" is gradeable; "tasteful lighting" is not.

**Grading posture.** You are grading someone else's work and the person who
asked wants the truth, not encouragement. A 3 is "acceptable and unremarkable".
Name the specific tile or element that is wrong, never a general impression.
